function registerCustomHandlers(bot) {
  // Добавляй сюда новые обработчики по мере развития проекта.
  // Пример:
  // bot.command("help", async (ctx) => {
  //   await ctx.reply("Справка по командам");
  // });
}

module.exports = {
  registerCustomHandlers,
};
